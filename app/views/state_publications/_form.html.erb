<%-#= form_with(model: state_publication, local: true) do |form| -%>

<%= form_for([@publication, @state_publication], remote: true, html: {id: "comments-form-publication", :"data-type" => "json"}) do |form| %>
  <% if @state_publication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state_publication.errors.count, "error") %> prohibited this state_publication from being saved:</h2>

      <ul>
      <% @state_publication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
