<%-#= form_with(model: comment_publication, local: true) do |form| -%>

<%= form_for([@publication, @comment_publication], remote: true, html: {id: "comments-form-publication", :"data-type" => "json"}) do |form| %>
  <% if @comment_publication.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment_publication.errors.count, "error") %> prohibited this comment_publication from being saved:</h2>

      <ul>
      <% @comment_publication.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :comment %>
    <%= form.text_area :comment %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
