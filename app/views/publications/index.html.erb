
<div class="body_container">

  
    <% @publications.each do |publication| %>

      <a href=<%= publication.id %> class="ref_section">

        <div class="general_container">

          <% if publication.photo.class != NilClass && publication.photo != '' %>

            <div class="image_description">
            <!--<img src="<%= publication.photo %>">-->
              <%= image_tag @publication.cover.url()%>
            </div>

            <div class="data_description">
              <h5><%= link_to publication.title, publication %></h5>
              <p><%= link_to publication.description, publication %></p>  
              <p><strong>Fecha publicación: </strong><%= publication.updated_at.year %>-<%= publication.updated_at.month %>-<%= publication.updated_at.day %></p>       

              <% unless publication.user.nil?%>
                <p>
                <strong>Publicado por:</strong>
                <%= publication.user.name%> 
                </p>
              <% end %>

            </div>

          <% else %>
            <div class="data_information">
              <h5><%= link_to publication.title, publication %></h5>
              <p><%= link_to publication.description, publication %></p>  
              <p><strong>Fecha publicación: </strong><%= publication.updated_at.year %>-<%= publication.updated_at.month %>-<%= publication.updated_at.day %></p>
              
              <% unless publication.user.nil?%>
                <p>
                <strong>Publicado por:</strong>
                <%= publication.user.name%> 
                </p>
              <% end %>
              
            </div>
          <% end %>

        </div>

      </a>

    <% end %>

</div>
